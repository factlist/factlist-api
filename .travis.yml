language: minimal

services:
  - docker

before_install:
  - DOCKER_TAG=factlist/factlist-api:${TRAVIS_BRANCH}

install:
  - docker build -t $DOCKER_TAG .

after_success:
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push $DOCKER_TAG